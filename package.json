{
  "name": "personal-site-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "upload-quizzes": "node scripts/upload-quizzes.mjs"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
